# Python Dependencies - Fixed versions for security and reproducibility
# Generated: 2026-06-10
# Note: Run 'pip install -r requirements.txt' to install exact versions

# Database
pymysql==1.1.3              # MySQL connector (installed version)

# Environment & Configuration
python-dotenv==1.2.2        # .env file loader (installed version)

# AI/ML Services
openai==2.33.0              # OpenAI API client (installed version)
openai-agents==0.17.2       # OpenAI Agents SDK (installed version)

# Audio/Video Processing
av==15.1.0                  # PyAV - FFmpeg bindings (stable version)
faster-whisper==1.2.1       # Faster Whisper transcription (stable version)