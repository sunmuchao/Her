# 发现页加号测评内容整理

> **整理时间**: 2026年6月2日
> **整理来源**: 发现页加号菜单 (`frontend/her-app/components/her/discover-page.tsx`)
> **整理目的**: 完整记录发现页加号菜单中所有测评内容,方便查阅和维护

---

## 📋 测评菜单概览

### 加号菜单位置

发现页底部输入框左侧的 **加号按钮 (+)** 点击后展开菜单

### 测评内容清单

加号菜单中包含 **心理测评** 折叠菜单,展开后显示 **4 种测评**:

| 序号 | 测评名称 | 图标 | 颜色主题 | 测评类型代码 |
|------|---------|------|---------|-------------|
| 1 | **MBTI** | 🧠 Brain | primary (主题色) | `mbti_16` |
| 2 | **依恋风格** | ❤️ Heart | rose (玫瑰色) | `attachment_style` |
| 3 | **恋爱语言** | ✨ Sparkles | gold (金色) | `love_language` |
| 4 | **价值观拍卖** | ✨ Sparkles (琥珀色) | amber-600 (琥珀色) | `values_auction` |

---

## 🧠 MBTI 测评

### 菜单入口设计

```tsx
<button
  onClick={() => {
    void openAssessmentCard('mbti_16')
    setShowActionMenu(false)
    setShowAssessmentSubmenu(false)
  }}
  disabled={assessmentBusy || valuesAuctionBusy || !userKey}
  className="flex flex-col items-center gap-1.5 p-3 rounded-xl bg-secondary/50 hover:bg-secondary transition-colors disabled:opacity-60"
  aria-label="MBTI测评"
>
  <div className="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center">
    <Brain className="w-5 h-5 text-primary" />
  </div>
  <span className="text-xs text-foreground">MBTI</span>
</button>
```

### 视觉设计规范

- **图标**: Brain (大脑图标)
- **图标容器**: 40px 圆形,背景色 `bg-primary/10` (主题色10%透明度)
- **图标颜色**: `text-primary` (主题色)
- **文字标签**: "MBTI",字号 `text-xs`
- **按钮容器**: 圆角矩形 `rounded-xl`,背景色 `bg-secondary/50`
- **交互效果**: hover时背景色变为 `bg-secondary`

### 测评基本信息

- **完整名称**: MBTI 16型人格测评
- **理论基础**: Myers-Briggs 类型指标
- **测评维度**: 
  - E/I (外向/内向) - 能量来源
  - S/N (实感/直觉) - 信息获取
  - T/F (思考/情感) - 决策方式
  - J/P (判断/知觉) - 生活态度
- **题目数量**: 20题 (每维度5题)
- **完成时长**: 约5分钟
- **输出结果**: 16型人格类型 + 恋爱说明书

### 测评价值主张

**用户价值**: 了解性格类型,知道恋爱中会吸引谁、抗拒谁

**核心输出**:
- 文艺昵称 (如:"情绪永动机")
- 网感昵称 (如:"快乐小狗")
- 5个行为标签
- 恋爱说明书 (优势、坑点、最佳匹配、需磨合类型)
- 小雅专属解读消息

---

## ❤️ 依恋风格测评

### 菜单入口设计

```tsx
<button
  onClick={() => {
    void openAssessmentCard('attachment_style')
    setShowActionMenu(false)
    setShowAssessmentSubmenu(false)
  }}
  disabled={assessmentBusy || valuesAuctionBusy || !userKey}
  className="flex flex-col items-center gap-1.5 p-3 rounded-xl bg-secondary/50 hover:bg-secondary transition-colors disabled:opacity-60"
  aria-label="依恋风格测评"
>
  <div className="w-10 h-10 rounded-full bg-rose/10 flex items-center justify-center">
    <Heart className="w-5 h-5 text-rose" />
  </div>
  <span className="text-xs text-foreground">依恋风格</span>
</button>
```

### 视觉设计规范

- **图标**: Heart (爱心图标)
- **图标容器**: 40px 圆形,背景色 `bg-rose/10` (玫瑰色10%透明度)
- **图标颜色**: `text-rose` (玫瑰色)
- **文字标签**: "依恋风格",字号 `text-xs`
- **按钮容器**: 圆角矩形 `rounded-xl`,背景色 `bg-secondary/50`
- **交互效果**: hover时背景色变为 `bg-secondary`

### 测评基本信息

- **完整名称**: 依恋风格测验 (12题精简版)
- **理论基础**: Bowlby 依恋理论 (1969), Hazan & Shaver (1987)
- **测评维度**: 四种依恋类型
  - **secure** (安全型) - 稳如老狗 (STBL)
  - **anxious** (焦虑型) - 腾博怨妇 (ANX-S)
  - **avoidant** (回避型) - 装死大师 (FAKE-D)
  - **fearful** (恐惧型) - 反复横跳狂魔 (WOC-F)
- **题目数量**: 12题 (每类型3题)
- **完成时长**: 约3分钟
- **输出结果**: 主要依恋类型 + 关系模式分析

### 测评价值主张

**用户价值**: 了解恋爱中的安全感来源

**核心输出**:
- SBTI 毒舌风格昵称 (如:"稳如老狗"、"腾博怨妇")
- 关系模式描述
- 关系雷区
- 适合对象分析
- 相处建议
- 小雅毒舌判官风格解读

### 四种依恋类型特征

| 类型 | SBTI昵称 | 核心特征 | 恋爱表现 |
|------|---------|---------|---------|
| **安全型** | 稳如老狗 (STBL) | 情绪稳定,能平衡亲密和独立 | 不黏也不冷,对象发疯你睡觉 |
| **焦虑型** | 腾博怨妇 (ANX-S) | 黏人,需要很多安全感 | 回消息慢5分钟已脑补出轨路线图 |
| **回避型** | 装死大师 (FAKE-D) | 冷暴力大师,需要很多空间 | 对象黏太紧立刻人间蒸发 |
| **恐惧型** | 反复横跳狂魔 (WOC-F) | 矛盾纠结体,既想黏又怕被伤害 | 前一秒想结婚后一秒'男人没一个好东西' |

---

## ✨ 恋爱语言测评

### 菜单入口设计

```tsx
<button
  onClick={() => {
    void openAssessmentCard('love_language')
    setShowActionMenu(false)
    setShowAssessmentSubmenu(false)
  }}
  disabled={assessmentBusy || valuesAuctionBusy || !userKey}
  className="flex flex-col items-center gap-1.5 p-3 rounded-xl bg-secondary/50 hover:bg-secondary transition-colors disabled:opacity-60"
  aria-label="恋爱语言测评"
>
  <div className="w-10 h-10 rounded-full bg-gold/10 flex items-center justify-center">
    <Sparkles className="w-5 h-5 text-gold" />
  </div>
  <span className="text-xs text-foreground">恋爱语言</span>
</button>
```

### 视觉设计规范

- **图标**: Sparkles (闪烁图标)
- **图标容器**: 40px 圆形,背景色 `bg-gold/10` (金色10%透明度)
- **图标颜色**: `text-gold` (金色)
- **文字标签**: "恋爱语言",字号 `text-xs`
- **按钮容器**: 圆角矩形 `rounded-xl`,背景色 `bg-secondary/50`
- **交互效果**: hover时背景色变为 `bg-secondary`

### 测评基本信息

- **完整名称**: 恋爱五种语言测验 (10题精简版)
- **理论基础**: Gary Chapman 五种恋爱语言 (1995)
- **测评维度**: 五种恋爱语言
  - **words_of_affirmation** (肯定言词) - 无成本白嫖大师
  - **quality_time** (精心时刻) - 网瘾戒断所教官
  - **receiving_gifts** (接受礼物) - 人形吞金兽
  - **acts_of_service** (服务行动) - 巨婴饲养员
  - **physical_touch** (身体接触) - 人形章鱼
- **题目数量**: 10题 (每语言2题)
- **完成时长**: 约2分钟
- **输出结果**: TOP5恋爱语言敏感度排序

### 测评价值主张

**用户价值**: 知道怎么表达爱对方才舒服

**核心输出**:
- 最敏感的爱的信号
- 不敏感的信号
- 让对方感受到爱的方法
- 对方不懂你的信号
- 小雅贴心建议风格解读

### 五种恋爱语言特征

| 语言 | SBTI昵称 | 核心需求 | 让TA开心的最快方式 |
|------|---------|---------|------------------|
| **肯定言词** | 无成本白嫖大师 | 需要被赞美、被鼓励、彩虹屁 | 夸TA一句'宝你真好看'就能让TA免费干三天家务 |
| **精心时刻** | 网瘾戒断所教官 | 需要专注的陪伴、深度对话 | 约会时敢看一眼手机,TA今天就别想活着走出餐厅 |
| **接受礼物** | 人形吞金兽 | 需要物质象征、小心意 | 不要问TA爱不爱你,看你送的包是不是限定款 |
| **服务行动** | 巨婴饲养员 | 需要实际帮助、对方帮你做事 | 核心需求是找个能帮TA通马桶交电费的爹系妈系工具人 |
| **身体接触** | 人形章鱼 | 需要肢体接触、抱抱贴贴 | 夏天体温39度也必须像胶水一样粘在对方身上 |

---

## ✨ 价值观拍卖会

### 菜单入口设计

```tsx
<button
  onClick={() => {
    void openValuesAuctionCard()
    setShowActionMenu(false)
    setShowAssessmentSubmenu(false)
  }}
  disabled={assessmentBusy || valuesAuctionBusy || !userKey}
  className="flex flex-col items-center gap-1.5 p-3 rounded-xl bg-secondary/50 hover:bg-secondary transition-colors disabled:opacity-60"
  aria-label="价值观拍卖会"
>
  <div className="w-10 h-10 rounded-full bg-amber-500/10 flex items-center justify-center">
    <Sparkles className="w-5 h-5 text-amber-600" />
  </div>
  <span className="text-xs text-foreground">价值观拍卖</span>
</button>
```

### 视觉设计规范

- **图标**: Sparkles (闪烁图标,琥珀色)
- **图标容器**: 40px 圆形,背景色 `bg-amber-500/10` (琥珀色10%透明度)
- **图标颜色**: `text-amber-600` (琥珀色)
- **文字标签**: "价值观拍卖",字号 `text-xs`
- **按钮容器**: 圆角矩形 `rounded-xl`,背景色 `bg-secondary/50`
- **交互效果**: hover时背景色变为 `bg-secondary`

### 测评基本信息

- **完整名称**: 价值观拍卖会 (游戏化测评)
- **理论基础**: 游戏化价值观探索
- **测评形式**: 用10个筹码竞拍20个人生拍品
- **完成时长**: 约2分钟
- **输出结果**: 价值观优先级排序 + 底层价值权重分布

### 测评价值主张

**用户价值**: 快速了解三观是否合拍

**核心输出**:
- TOP3最看重的拍品
- 底层价值权重分布
- 价值观类型分类 (如:安全感至上型、自由灵魂型)
- 小雅价值观解读

### 游戏化设计特点

**从问卷感到游戏感**:
- 用户看到的是"拍品",不是"价值观标签"
- 拍品之间必须有冲突 (如:百亿上市公司 vs 内心平静)
- 前台玩拍品,后台算价值观
- 取舍中暴露真实价值排序

---

## 🎨 菜单UI设计总结

### 折叠菜单设计

```tsx
{/* 心理测评折叠按钮 */}
<button
  onClick={() => setShowAssessmentSubmenu(!showAssessmentSubmenu)}
  className="flex items-center justify-between w-full p-3 rounded-xl bg-secondary hover:bg-secondary/80 transition-colors"
  aria-expanded={showAssessmentSubmenu}
  aria-label="心理测评"
>
  <div className="flex items-center gap-3">
    <div className="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center">
      <ClipboardList className="w-5 h-5 text-primary" />
    </div>
    <span className="text-sm font-medium text-foreground">心理测评</span>
  </div>
  <ChevronDown 
    className={cn(
      "w-5 h-5 text-muted-foreground transition-transform duration-200",
      showAssessmentSubmenu && "rotate-180"
    )} 
  />
</button>
```

### 展开动画效果

```tsx
{showAssessmentSubmenu && (
  <div className="mt-2 grid grid-cols-2 gap-3 animate-fade-in-up">
    {/* 4个测评按钮 */}
  </div>
)}
```

- **布局**: 2列网格布局 `grid grid-cols-2`
- **间距**: `gap-3` (12px)
- **动画**: `animate-fade-in-up` (淡入上升动画)
- **展开图标旋转**: ChevronDown 旋转180度

### 颜色体系

| 测评类型 | 背景色 | 图标色 | Tailwind类名 |
|---------|-------|-------|-------------|
| **心理测评(折叠)** | primary/10 | primary | `bg-primary/10 text-primary` |
| **MBTI** | primary/10 | primary | `bg-primary/10 text-primary` |
| **依恋风格** | rose/10 | rose | `bg-rose/10 text-rose` |
| **恋爱语言** | gold/10 | gold | `bg-gold/10 text-gold` |
| **价值观拍卖** | amber-500/10 | amber-600 | `bg-amber-500/10 text-amber-600` |

---

## 🔧 技术实现细节

### 打开测评函数

```typescript
const openAssessmentCard = async (
  assessmentType: 'mbti_16' | 'attachment_style' | 'love_language' = 'mbti_16'
) => {
  if (!userKey || assessmentBusy) return
  
  setAssessmentBusy(true)          // 设置忙碌状态
  setCurrentAssessmentType(assessmentType) // 设置当前测评类型
  setValuesAuctionCard(null)       // 清空价值观拍卖卡片
  removeSuggestedActions()         // 移除建议操作
  
  try {
    const intro = await startAssessment(userKey, assessmentType)
    setAssessmentId(intro.assessment_id)
    setAssessmentCard(intro)
    setAssessmentQuestionHistory([])
    
    // 打开测评卡片后,滚动到底部
    setTimeout(() => {
      chatEndRef.current?.scrollIntoView({ behavior: 'smooth' })
    }, 150)
  } catch (error) {
    const assessmentNames = {
      'mbti_16': 'MBTI',
      'attachment_style': '依恋风格',
      'love_language': '恋爱语言'
    }
    notifyError(error, `打开 ${assessmentNames[assessmentType]} 测评失败`)
  } finally {
    setAssessmentBusy(false)
  }
}
```

### 打开价值观拍卖函数

```typescript
const openValuesAuctionCard = async () => {
  if (!userKey || valuesAuctionBusy) return
  
  setValuesAuctionBusy(true)       // 设置忙碌状态
  setAssessmentCard(null)          // 清空测评卡片
  removeSuggestedActions()         // 移除建议操作
  
  try {
    const intro = await startValuesAuction(userKey)
    setValuesAuctionCard(intro)
    
    setTimeout(() => {
      chatEndRef.current?.scrollIntoView({ behavior: 'smooth' })
    }, 150)
  } catch (error) {
    notifyError(error, '打开价值观拍卖会失败')
  } finally {
    setValuesAuctionBusy(false)
  }
}
```

### 状态管理

```typescript
// 测评状态
const [assessmentCard, setAssessmentCard] = useState<AssessmentCard | null>(null)
const [assessmentId, setAssessmentId] = useState<string | null>(null)
const [currentAssessmentType, setCurrentAssessmentType] = useState<'mbti_16' | 'attachment_style' | 'love_language'>('mbti_16')
const [assessmentQuestionHistory, setAssessmentQuestionHistory] = useState<AssessmentQuestionCard['question_data'][]>([])
const [assessmentBusy, setAssessmentBusy] = useState(false)

// 价值观拍卖会状态
const [valuesAuctionCard, setValuesAuctionCard] = useState<ValuesAuctionCard | null>(null)
const [valuesAuctionBusy, setValuesAuctionBusy] = useState(false)

// 菜单状态
const [showActionMenu, setShowActionMenu] = useState(false)
const [showAssessmentSubmenu, setShowAssessmentSubmenu] = useState(false)
```

### API端点

```typescript
// 测评相关API
import {
  answerAssessment,
  beginAssessment,
  startAssessment,
  getXiaoyaMessage,
  addAssessmentLabels,
  addXiaoyaMessageToDiscovery,
} from '@/lib/api/endpoints/assessment'

// 价值观拍卖会API
import {
  getValuesAuctionInterpretation,
  getValuesAuctionLots,
  startValuesAuction,
  submitValuesAuctionBids,
} from '@/lib/api/endpoints/valuesAuction'
```

---

## 📊 测评流程统一

### 所有测评遵循统一流程

```
介绍卡片 → 开始测评 → 问题卡片 → 答题 → 反馈卡片 → 结果卡片 → 小雅解读 → 继续聊天
```

### 测评卡片渲染

使用统一的 `AssessmentCardRenderer` 组件:

```tsx
<AssessmentCardRenderer
  card={assessmentCard}
  onStart={async () => {
    const next = await beginAssessment(assessmentId)
    setAssessmentCard(next)
  }}
  onAnswer={async (answer) => {
    // 答题逻辑
  }}
  onContinue={async () => {
    // 继续逻辑
  }}
  onContinueChat={() => {
    clearAssessmentCard()
  }}
  onAddLabels={handleAddLabels}
  assessmentType={currentAssessmentType}
/>
```

### 价值观拍卖卡片渲染

使用 `ValuesAuctionCardRenderer` 组件:

```tsx
<ValuesAuctionCardRenderer
  card={valuesAuctionCard}
  userKey={userKey}
  onStart={async () => {
    const next = await getValuesAuctionLots(valuesAuctionCard.assessment_id)
    setValuesAuctionCard(next)
  }}
  onSubmitBids={async (bids) => {
    const next = await submitValuesAuctionBids({
      assessmentId: valuesAuctionCard.assessment_id,
      userKey,
      bids,
    })
    setValuesAuctionCard(next)
  }}
  onViewInterpretation={async () => {
    const next = await getValuesAuctionInterpretation({
      assessmentId: valuesAuctionCard.assessment_id,
      userKey,
    })
    setValuesAuctionCard(next)
  }}
  onContinue={clearValuesAuctionCard}
/>
```

---

## 💡 小雅解读风格对比

### 不同测评的小雅解读风格

| 测评类型 | 解读风格 | 开场白示例 |
|---------|---------|-----------|
| **MBTI** | 网感口语化 | "亲爱的,你的测试结果出来啦!🎉 你是ENFP——传说中的「快乐小狗」!" |
| **依恋风格** | 毒舌判官风 | "测出来了,你是「稳如老狗 (STBL)」。🎉 情绪灭火器,对象发疯你睡觉,主打一个无动于衷。" |
| **恋爱语言** | 贴心建议风 | "亲爱的,我发现了一个让你更开心的秘密 💡 XX让你最心动,对方用这种方式表达爱你会感动到哭。" |
| **价值观拍卖** | 价值观解读风 | (具体风格待补充) |

---

## 🎯 测评匹配价值

### 每个测评在匹配中的应用

| 测评类型 | 匹配应用场景 |
|---------|------------|
| **MBTI** | 性格互补/相似匹配,避免性格冲突 |
| **依恋风格** | 避免追逐-逃跑恶性循环,找适合的安全感来源 |
| **恋爱语言** | 让对方感受到爱,避免表达方式错位 |
| **价值观拍卖** | 三观契合/冲突分析,避免根本性价值观冲突 |

---

## 📝 总结

### 发现页加号测评核心特点

1. **入口统一**: 都在发现页加号菜单的心理测评子菜单中
2. **UI统一**: 都使用相同的按钮布局和视觉规范
3. **流程统一**: 都使用测评卡片渲染器,遵循相同的测评流程
4. **风格统一**: 都使用SBTI毒舌风格 + 小雅专属解读
5. **价值统一**: 都服务于匹配增强和关系建议

### 四种测评差异化

| 维度 | MBTI | 依恋风格 | 恋爱语言 | 价值观拍卖 |
|------|------|---------|---------|-----------|
| **图标** | Brain | Heart | Sparkles(gold) | Sparkles(amber) |
| **颜色** | primary | rose | gold | amber |
| **题目数** | 20题 | 12题 | 10题 | 无固定题目 |
| **完成时长** | 5分钟 | 3分钟 | 2分钟 | 2分钟 |
| **测什么** | 16型性格 | 安全感来源 | 爱的表达偏好 | 人生价值排序 |
| **输出形式** | 性格类型说明书 | 依恋类型分析 | TOP5敏感度排序 | 价值观权重分布 |

---

**文档整理完成 ✅**

> **核心要点**:
> - 发现页加号菜单包含4种心理测评
> - UI设计统一规范,颜色差异化区分
> - 测评流程统一,卡片渲染器统一
> - SBTI毒舌风格贯穿所有测评
> - 所有测评服务于匹配增强和关系建议