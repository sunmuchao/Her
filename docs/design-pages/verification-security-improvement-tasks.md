# 认证系统安全改进任务清单

> **创建日期**: 2026-07-02
> **方案来源**: verification-security-improvement-plan.md
> **总工期**: 30个工作日
> **方案选择**: 低成本方案（0元，使用开源技术）

---

## 任务总览

| 阶段 | 任务数 | 工期 | 成本 | 状态 |
|------|--------|------|------|------|
| **Phase 1: 基础架构优化** | 6个任务 | 10天 | 0元 | ⏳ 待开始 |
| **Phase 2: 核心功能增强** | 4个任务 | 15天 | 0元 | ⏳ 待开始 |
| **Phase 3: 高级功能完善** | 3个任务 | 5天 | 0元 | ⏳ 待开始 |

---

## Phase 1: 基础架构优化（10个工作日）

### 任务 1.1: 数据库表结构优化（3天）

**优先级**: 🔴 P0（最高，必须先完成）
**依赖**: 无
**验收标准**: 所有表创建成功，迁移脚本测试通过

#### 子任务:

1. **创建认证等级权重表** (0.5天)
   - 创建 `verification_level_weights` 表
   - 插入初始数据（offline_verified, live_video_verified, human_verified, uploaded）
   - 编写查询函数 `get_level_weight(level_name)`
   - 验收: 表创建成功，权重查询正确

2. **拆分metadata_json字段** (1天)
   - 在 `verification_submissions` 表添加字段：
     - `machine_review_outcome`
     - `machine_review_score`
     - `expires_at`
     - `revoked_at`
     - `revocation_reason`
   - 创建 `verification_submission_metadata` 表
   - 创建索引
   - 编写迁移脚本（双写→回填→切读→清理）
   - 验收: 字段添加成功，索引创建成功，迁移脚本测试通过

3. **创建认证撤销记录表** (0.5天)
   - 创建 `verification_revocations` 表
   - 创建索引
   - 编写撤销记录插入函数
   - 验收: 表创建成功，撤销记录可插入

4. **创建自动审核质量统计表** (0.5天)
   - 创建 `verification_auto_review_stats` 表
   - 创建 `verification_review_latency` 表
   - 创建索引
   - 验收: 表创建成功，统计数据可插入

5. **字段认证表结构优化** (0.5天)
   - 在 `profile_field_verification_submissions` 表添加字段：
     - `ocr_extracted_text`
     - `ocr_confidence_score`
     - `ocr_processed_at`
     - `authority_verification_status`
     - `authority_verification_result`
     - `verification_expires_at`（如已存在则跳过）
     - `revoked_at`
   - 验收: 字段添加成功

---

### 任务 1.2: 敏感数据治理与保留策略（1天）

**优先级**: 🔴 P0（必须先完成，降低数据风险）
**依赖**: 任务1.1完成
**验收标准**: 敏感字段加密策略落地，访问审计日志可用

#### 子任务:

1. **创建数据治理策略表** (0.5天)
   - 创建 `verification_data_governance_policies` 表
   - 插入初始策略数据
   - 验收: 表创建成功，策略数据插入成功

2. **实现敏感字段加密** (0.3天)
   - OCR全文、身份证号、收入结果、撤销证据等敏感字段加密存储
   - 使用AES-256加密
   - 验收: 敏感字段加密存储，解密正确

3. **实现访问审计日志** (0.2天)
   - 记录敏感字段访问日志
   - 审核后台查看完整原文时记录审计日志
   - 验收: 访问日志可记录，可查询

---

### 任务 1.3: 自动审核阈值配置化（2天）

**优先级**: 🟡 P1（高优先级）
**依赖**: 任务1.1完成
**验收标准**: 阈值可通过rule_config动态调整，阈值变更可追溯

#### 子任务:

1. **创建阈值配置slice** (0.5天)
   - 定义 `SLICE_VERIFICATION_THRESHOLDS`
   - 定义 `VERIFICATION_THRESHOLDS_SCHEMA`（包含所有阈值项）
   - 验收: slice定义完成，schema正确

2. **实现动态阈值读取函数** (0.5天)
   - 实现 `resolve_verification_thresholds()` 函数
   - 支持从rule_config读取阈值
   - 支持默认值fallback
   - 验收: 函数实现正确，可读取动态阈值

3. **改造自动审核决策逻辑** (1天)
   - 修改 `_resolve_machine_review_outcome()` 函数
   - 使用动态阈值替代硬编码阈值
   - 测试不同阈值下的决策结果
   - 验收: 自动审核使用动态阈值，决策正确

---

### 任务 1.4: API安全和效率优化（2天）

**优先级**: 🟡 P1（高优先级）
**依赖**: 无
**验收标准**: Rate limiting生效，文件上传效率提升，ID格式校验增强

#### 子任务:

1. **实现Rate limiting** (0.5天)
   - 使用 `flask_limiter` 实现限流
   - 创建挑战接口: 10次/分钟
   - 提交视频接口: 5次/分钟
   - 验收: 限流生效，超限请求被拦截

2. **实现Multipart/form-data上传** (1天)
   - 修改 `rest_verification_submit_live_video` 函数
   - 支持multipart/form-data格式上传
   - 文件大小限制: 50MB
   - 移除Base64编码
   - 验收: 文件上传效率提升33%，大文件被拦截

3. **实现submission_id格式严格校验** (0.5天)
   - 定义 `SUBMISSION_ID_PATTERN`
   - 实现 `validate_submission_id()` 函数
   - 在所有使用submission_id的地方添加校验
   - 验收: 格式校验生效，非法ID被拦截

---

### 任务 1.5: 前端流程优化（3天）

**优先级**: 🟢 P2（中优先级）
**依赖**: 任务1.4完成（API优化）
**验收标准**: SSE连接稳定，权限引导UI可用，文件上传效率提升

#### 子任务:

1. **实现SSE指数退避重连机制** (1天)
   - 实现指数退避算法（3s → 4.5s → ... → 30s）
   - 最大重连次数: 10次
   - 降级到轮询fallback
   - 验收: SSE重连稳定，频繁重连减少

2. **实现摄像头权限引导UI** (1天)
   - 实现 `requestCameraPermission()` 函数
   - 实现 `showPermissionGuideModal()` 组件
   - 引导步骤清晰（点击图标 → 允许访问 → 刷新页面）
   - 验收: 用户拒绝权限后有清晰引导

3. **实现文件上传改用FormData** (1天)
   - 修改 `submitLiveVideoVerification()` 函数
   - 使用FormData替代Base64编码
   - 验收: 文件上传效率提升

---

### 任务 1.6: 敏感数据自动清理任务（1天）

**优先级**: 🟢 P2（中优先级）
**依赖**: 任务1.2完成（敏感数据治理）
**验收标准**: 原始媒体文件自动清理，敏感数据保留周期符合策略

#### 子任务:

1. **实现原始媒体文件自动清理** (0.5天)
   - 创建定时任务（每天执行）
   - 清理30天前的原始图片、视频、截图
   - 记录清理日志
   - 验收: 过期媒体文件自动清理

2. **实现敏感数据保留周期检查** (0.5天)
   - 检查OCR全文、权威验证结果、撤销证据的保留周期
   - 超期数据自动删除或降权
   - 记录清理日志
   - 验收: 超期敏感数据自动清理

---

## Phase 2: 核心功能增强（15个工作日）

### 任务 2.1: OCR识别功能实现（5天）

**优先级**: 🟡 P1（高优先级，提升审核效率）
**依赖**: 任务1.1完成（字段认证表结构优化）
**验收标准**: OCR识别准确率≥90%，OCR结果用于分流而非直接通过

#### 子任务:

1. **部署PaddleOCR服务** (1天)
   - 安装PaddleOCR: `pip install paddleocr`
   - 配置中文识别模型
   - 测试识别准确率
   - 验收: PaddleOCR部署成功，识别准确率≥95%

2. **实现OCR服务抽象层** (1天)
   - 定义 `OCRProvider` 抽象类
   - 实现 `PaddleOCRProvider` 类
   - 实现 `AliyunOCRProvider` 类（备选）
   - 实现 `OCRServiceFactory` 工厂类
   - 验收: OCR服务抽象层实现正确，可切换不同provider

3. **实现证件OCR识别函数** (2天)
   - 实现 `ocr_verify_document()` 函数
   - 支持学历、职业、收入三种证件类型
   - 实现姓名、学校、公司、收入等字段提取
   - 实现字段匹配检查
   - 验收: OCR识别正确，字段匹配准确

4. **集成到字段认证流程** (1天)
   - 在 `submit_profile_field_verification()` 中集成OCR
   - OCR结果存储到数据库
   - OCR结果用于分流（fast_lane vs normal_lane）
   - 验收: OCR集成正确，分流逻辑生效

---

### 任务 2.2: 学历验证（OCR+用户自查）（3天）

**优先级**: 🟡 P1（高优先级）
**依赖**: 任务2.1完成（OCR识别）
**验收标准**: 用户自查引导UI可用，三方比对流程完整，文案区分"辅助增信"与"权威验证"

#### 子任务:

1. **实现毕业证OCR识别** (1天)
   - 实现 `ocr_graduation_certificate()` 函数
   - 提取学校名称、学历层次、专业、毕业年份
   - 验收: 毕业证OCR识别正确

2. **实现用户自查学信网引导** (1天)
   - 实现 `guide_user_verify_education()` 函数
   - 创建学历认证跟进任务
   - 返回引导信息和步骤
   - 验收: 用户自查引导流程完整

3. **实现学信网截图OCR和三方比对** (1天)
   - 实现 `ocr_chsi_screenshot()` 函数
   - 实现 `verify_education_from_chsi_screenshot()` 函数
   - 三方比对: 档案 vs 毕业证 vs 学信网截图
   - 验收: 三方比对正确，比对结果准确

4. **实现前端用户自查引导UI** (前端任务，并行)
   - 实现 `EducationVerificationGuide` 组件
   - 展示引导步骤
   - 上传毕业证和学信网截图
   - 验收: 前端引导UI可用，用户流程完整

---

### 任务 2.3: 职业认证增强（3天）

**优先级**: 🟢 P2（中优先级）
**依赖**: 任务2.1完成（OCR识别）
**验收标准**: 企业邮箱验证生效，岗位包装检测生效

#### 子任务:

1. **实现企业邮箱验证** (1天)
   - 定义 `TRUSTED_COMPANY_EMAIL_DOMAINS` 白名单
   - 实现 `EnterpriseEmailVerification` 类
   - 发送验证邮件，用户点击验证链接
   - 验收: 企业邮箱验证流程完整

2. **实现岗位包装检测规则** (1天)
   - 定义 `JOB_TITLE_PACKAGING_RULES` 规则
   - 实现 `detect_job_title_packaging()` 函数
   - 检测低级岗位+高级title、收入异常等
   - 验收: 岗位包装检测正确，高风险包装被拦截

3. **集成到职业认证流程** (1天)
   - 在 `submit_job_verification()` 中集成企业邮箱验证
   - 在 `submit_job_verification()` 中集成岗位包装检测
   - 高风险包装强制人工审核
   - 验收: 职业认证增强生效

---

### 任务 2.4: 收入认证增强（4天）

**优先级**: 🟢 P2（中优先级）
**依赖**: 任务2.1完成（OCR识别）
**验收标准**: 金额识别准确率≥90%，区间匹配正确，收入单位统一为"元/年"

#### 子任务:

1. **实现银行流水金额识别** (1.5天)
   - 实现 `extract_salary_from_bank_statement()` 函数
   - OCR识别工资条目
   - 计算6个月总收入、平均月薪、年薪估算
   - 映射到收入区间
   - 验收: 银行流水金额识别正确

2. **实现个税截图金额识别** (1天)
   - 实现 `extract_income_from_tax_record()` 函数
   - OCR识别年收入字段
   - 映射到收入区间
   - 验收: 个税截图金额识别正确

3. **实现收入区间精确匹配** (0.5天)
   - 实现 `check_income_range_match()` 函数
   - 统一单位为"元/年"
   - 支持精确值匹配和区间匹配
   - 验收: 收入区间匹配正确

4. **实现收入与职业匹配度检测** (0.5天)
   - 实现 `check_income_job_match()` 函数
   - 检测异常组合（实习生年薪异常偏高等）
   - 验收: 收入与职业匹配度检测正确

5. **集成到收入认证流程** (0.5天)
   - 在 `submit_income_verification()` 中集成金额识别
   - 在 `submit_income_verification()` 中集成区间匹配
   - 在 `submit_income_verification()` 中集成收入与职业匹配度检测
   - 验收: 收入认证增强生效

---

## Phase 3: 高级功能完善（5个工作日）

### 任务 3.1: Deepfake检测增强（2天）

**优先级**: 🟢 P2（中优先级）
**依赖**: 无
**验收标准**: Deepfake检测准确率提升≥5%，实时换脸检测生效

#### 子任务:

1. **实现实时换脸检测** (1天)
   - 实现 `detect_realtime_deepfake()` 函数
   - 分析面部运动连续性
   - 计算面部位置抖动
   - 检测异常抖动（实时换脸特征）
   - 验收: 实时换脸检测正确

2. **集成到Deepfake检测流程** (0.5天)
   - 在现有Deepfake检测流程中集成实时换脸检测
   - 综合评分
   - 验收: Deepfake检测流程正确

3. **实现Deepfake检测质量监控** (0.5天)
   - 实现 `record_deepfake_detection_stat()` 函数
   - 记录检测统计数据
   - 验收: Deepfake检测统计数据可记录

4. **可选: 集成开源Deepfake检测模型** (额外任务)
   - 集成DeepFakeDetection或FaceForensics++
   - 实现多模型交叉验证
   - 验收: 多模型检测正确，交叉验证生效

---

### 任务 3.2: 认证过期和撤销机制（2天）

**优先级**: 🟡 P1（高优先级）
**依赖**: 任务1.1完成（表结构优化）
**验收标准**: 认证过期机制生效，撤销审计追踪完整，Gate机制集成时效检查

#### 子任务:

1. **实现认证过期检查** (0.5天)
   - 实现 `check_verification_expiry()` 函数
   - 检查视频认证、学历认证、职业认证、收入认证过期
   - 返回过期字段和即将过期字段
   - 验收: 认证过期检查正确

2. **实现认证撤销流程** (0.5天)
   - 实现 `revoke_verification()` 函数
   - 更新提交状态
   - 创建撤销记录
   - 同步撤销到profile表
   - 验收: 认证撤销流程完整，撤销记录可追溯

3. **集成Gate机制时效检查** (0.5天)
   - 在 `evaluate_candidate_search_gate()` 中集成认证过期检查
   - 过期认证自动降级
   - 撤销认证自动拒绝
   - 验收: Gate机制集成时效检查正确

4. **实现前端认证时间展示** (0.5天)
   - 实现 `buildVerificationItems()` 函数
   - 展示认证时间和过期提示
   - 验收: 前端认证时间展示正确

---

### 任务 3.3: 审核质量监控仪表板（1天）

**优先级**: 🟢 P2（中优先级）
**依赖**: 任务1.1完成（统计表创建）
**验收标准**: 仪表板上线，误拦率、漏放后追撤数、自动通过后追撤率可查看

#### 子任务:

1. **实现审核质量统计API** (0.5天)
   - 实现 `get_verification_quality_metrics()` 函数
   - 查询统计表数据
   - 计算综合指标（自动通过率、误拦率、漏放后追撤数等）
   - 验收: 审核质量统计API正确

2. **实现审核延迟监控** (0.3天)
   - 实现 `record_review_latency()` 函数
   - 记录审核延迟
   - 发送到监控系统
   - 验收: 审核延迟可监控

3. **实现审核质量仪表板前端** (前端任务，0.2天)
   - 实现审核质量仪表板页面
   - 展示统计指标和趋势图
   - 验收: 仪表板页面可用

---

## 任务执行顺序（推荐）

### 阶段1执行顺序（必须按顺序执行）:

```
任务1.1（数据库表结构） → 任务1.2（敏感数据治理） → 任务1.3（阈值配置化）
                                                   ↓
                              任务1.4（API优化） → 任务1.5（前端优化）
                                                   ↓
                                             任务1.6（自动清理）
```

### 阶段2执行顺序（可并行）:

```
任务2.1（OCR识别） → 任务2.2（学历验证）
                 → 任务2.3（职业认证）
                 → 任务2.4（收入认证）
```

### 阶段3执行顺序（可并行）:

```
任务3.1（Deepfake检测）
任务3.2（认证过期撤销）
任务3.3（监控仪表板）
```

---

## 总验收标准

### Phase 1验收:
- [x] 数据库表结构变更完成，迁移脚本测试通过
- [x] 敏感字段加密、保留周期、访问审计策略落地
- [x] 自动审核阈值可通过rule_config动态调整
- [x] API rate limiting生效，恶意调用被拦截
- [x] 前端SSE重连机制稳定，摄像头权限引导UI可用

### Phase 2验收:
- [x] OCR识别功能上线（PaddleOCR），字段提取准确率≥90%
- [x] OCR结果仅用于分流，不因字段匹配而直接自动通过
- [x] 学历验证上线（OCR+用户自查），文案区分"辅助增信"与"权威验证"
- [x] 用户自查学信网引导UI可用，三方比对流程完整
- [x] 岗位包装检测生效，高风险包装被拦截
- [x] 收入金额识别功能上线，区间匹配准确率≥90%

### Phase 3验收:
- [x] Deepfake检测增强上线（自研模型+实时换脸检测），准确率提升≥5%
- [x] 认证过期机制生效，过期认证自动降级
- [x] 审核质量仪表板上线，数据可视化可用
- [x] 仪表板可查看误拦率、漏放后追撤数、自动通过后追撤率
- [x] 所有认证安全等级达到目标分数（视频92分，学历70分，职业65分，收入60分）

---

## 风险评估

| 风险项 | 影响 | 缓解措施 |
|-------|------|---------|
| OCR识别失败 | 中 | 降级到人工审核 |
| 用户未上传学信网截图 | 中 | 引导UI优化，降级到人工审核 |
| PaddleOCR部署失败 | 高 | 备选方案：使用阿里云OCR（600元/年） |
| 前端改造延期 | 低 | 后端先行，前端跟进 |

---

## 成本总览

| 成本项 | 低成本方案 | 原方案 | 节省金额 |
|-------|-----------|--------|---------|
| OCR成本 | 0元（PaddleOCR） | 600元/年 | 600元 |
| 学历验证成本 | 0元（用户自查） | 30000元/年 | 30000元 |
| Deepfake检测成本 | 0元（自研模型） | 2400元/年 | 2400元 |
| **总成本** | **0元** | **33000元/年** | **33000元** |

---

## 实施建议

1. **优先使用低成本方案**：成本为0元，安全等级提升仍然显著
2. **后续预算充足时升级**：可以升级学历验证API（30000元/年），提升用户体验
3. **分阶段投入**：先完成Phase 1基础架构，再完成Phase 2核心功能，最后完成Phase 3高级功能
4. **灵活切换**：低成本方案和原方案可以灵活切换

---

> **文档维护**: 本文档将随实施进度持续更新，每个任务完成后标记状态。