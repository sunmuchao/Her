# 召回质量改进方案

## 背景

当前向量召回已经具备基础可用性，但实际测试表明：

- 对具体表达，效果较好
  - 例如：`及时回复`、`事事有回应`、`情绪稳定`、`善沟通`、`细腻`、`有耐心`、`生活规律`、`不要太卷`
- 对抽象表达和复合表达，效果一般
  - 例如：`上进心`、`认真推进关系`

问题不主要出在向量检索本身，而是出在“入库文本不标准、语义过于混杂、查询没有标准化”。

---

## 目标

把当前“召回一般”的查询，提升到“召回更准、更稳定、更可控”。

核心目标：

1. 同一个意思，入库表达尽量统一
2. 不同语义维度，尽量不要混在同一个摘要字段里
3. 查询前先做标准化，而不是原句直接裸查
4. 为后续增加标签层和更细的召回路由预留接口

---

## 当前问题拆解

### 1. 同义表达太散

例如用户搜：

- `有上进心`

库里实际可能写成：

- `有责任感`
- `工作稳定`
- `目标感强`
- `做事有条理`

这些语义接近，但不是统一表达。结果就是：

- 向量能大致召回
- 但召回会发散
- top 结果容易“差不多对”，不够“非常准”

### 2. `partner_expectation` 语义过载

当前 `partner_expectation` 常同时混入：

- 性格偏好
- 关系推进节奏
- 生活方式偏好
- 现实条件偏好

这会导致：

- 搜性格时容易被生活方式干扰
- 搜关系节奏时容易被性格词稀释

### 3. 查询原句直接向量化

例如：

- `我喜欢慢热，真诚，认真推进关系的`

现在基本是整句直接 embedding。

问题是：

- 口语里会有噪音
- 抽象词不稳定
- 同一句里可能混多个维度

结果是召回方向对，但精度不稳。

---

## 总体方案

本轮先落地“收益最大、改动最小”的两层标准化能力：

1. 摘要入库前标准化
2. 查询前标准化 + 字段路由

后续再继续扩到：

3. 标签层
4. 字段拆槽
5. 召回回归测试集

---

## 方案一：摘要入库前标准化

### 目标

把自然语言摘要变成更稳定的检索表达。

### 原则

- 不篡改原意
- 只做“近义表达统一”
- 只针对召回价值高的关键词
- 保留原始摘要，同时生成标准化文本供向量使用

### 优先标准化词

#### 性格偏好

- `温柔` -> `温和`
- `好脾气` -> `情绪稳定`
- `会沟通` / `好沟通` -> `善沟通`
- `成熟稳重` -> `稳重`

#### 成长驱动 / 上进心

- `上进心` -> `目标感强`
- `有事业心` -> `目标感强`
- `有冲劲` -> `成长驱动强`
- `愿意努力` -> `成长驱动强`

#### 关系推进

- `认真推进关系` -> `关系推进明确`
- `奔着恋爱去` -> `关系推进明确`
- `不想暧昧` -> `不暧昧`
- `愿意投入关系` -> `持续投入关系`

#### 生活节奏

- `不要太卷` -> `排斥高压内卷`
- `生活规律` -> `作息规律`
- `稳定一点` -> `生活稳定`

### 落地方式

新增一层标准化函数：

- 输入：原始摘要字段文本
- 输出：
  - `normalized_text`
  - `applied_rules`

先只对以下字段生效：

- `partner_expectation`
- `personality_traits`
- `life_attitude`
- `emotional_needs`

---

## 方案二：查询前标准化

### 目标

不要拿用户原句直接查，而是先改写成更适合检索的表达。

### 示例

#### 示例 1

原查询：

- `我希望找一个性格温柔，有上进心的`

标准化后：

- `温和，目标感强`

#### 示例 2

原查询：

- `我喜欢慢热，真诚，认真推进关系的`

标准化后：

- `慢热，真诚，关系推进明确`

#### 示例 3

原查询：

- `我希望对方生活规律，不要太卷`

标准化后：

- `作息规律，排斥高压内卷`

### 落地方式

在查询向量生成前新增标准化函数：

- 输入：查询文本
- 输出：
  - `normalized_query_text`
  - `expanded_terms`
  - `route_vector_types`

---

## 方案三：字段路由

### 目标

不同查询，优先查不同向量字段，不要所有查询都平均打到所有字段。

### 路由建议

#### 情感需求类

关键词：

- `及时回复`
- `有回应`
- `不冷处理`

优先字段：

- `emotional_needs`
- 其次 `partner_expectation`

#### 性格偏好类

关键词：

- `温和`
- `细腻`
- `有耐心`
- `善沟通`
- `独立`
- `边界感`

优先字段：

- `partner_expectation`
- `personality_traits`

#### 生活节奏类

关键词：

- `生活规律`
- `作息规律`
- `不要太卷`
- `稳定`

优先字段：

- `life_attitude`
- 其次 `partner_expectation`

#### 关系推进类

关键词：

- `不暧昧`
- `认真推进关系`
- `愿意投入`
- `节奏明确`

优先字段：

- `partner_expectation`
- `emotional_needs`

---

## 本轮落地范围

本轮直接落地以下内容：

1. 文档沉淀
2. 摘要入库前标准化函数
3. 查询前标准化函数
4. 基础字段路由函数
5. 单元测试

本轮不直接落地：

1. 新增数据库标签列
2. 改 Milvus schema
3. 拆新表 / 大规模数据迁移
4. 全量历史回写

---

## 预期收益

### 对“召回一般”的查询

#### `温柔，有上进心`

预期从：

- `温和 + 有责任感 + 工作稳定` 这类近义混召回

提升到：

- `温和 + 目标感强` 相关文本更靠前

#### `慢热，真诚，认真推进关系`

预期从：

- `慢热，真诚` 命中较好，但“认真推进关系”不稳定

提升到：

- 能更稳定召回 `关系推进明确`、`不暧昧`、`持续投入关系`

---

## 验收方式

用固定查询集做回归：

1. `我希望找一个性格温柔，有上进心的`
2. `我喜欢慢热，真诚，认真推进关系的`
3. `我需要对方及时回复，事事有回应`
4. `我希望对方生活规律，不要太卷`
5. `我喜欢独立，有边界感的`

验收标准：

- `准`：top 结果直接命中核心语义
- `一般`：命中大方向，但缺关键点
- `偏`：大部分结果不对题

目标是把当前的：

- `温柔，有上进心`：`一般`
- `慢热，真诚，认真推进关系`：`一般`

提升到更接近 `准`。
